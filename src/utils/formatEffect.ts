// Retired: RolledEffect gear schema replaced by GearPoolEntry in v0.5-real-gear.
// Real gear items carry name, tier, and flavor_text directly — no rolled effects to format.
// File retained to avoid breaking git history; safe to delete after v0.6.
export {};
